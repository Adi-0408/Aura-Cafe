rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // All collections: permit seamless database synchronization
    match /{document=**} {
      allow read, write: if true;
    }

  }
}

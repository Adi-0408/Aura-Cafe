import { LiveOrder } from '../types';

// Empty default sales ledger - only real live orders from Cloud Firestore and active counter registers will populate
export const INITIAL_SAMPLE_SALES_LEDGER: LiveOrder[] = [];
